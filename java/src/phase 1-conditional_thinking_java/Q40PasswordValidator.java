import java.util.Scanner;

/**
 * Phase 1: Conditional Thinking (If-Else, Boolean Logic)
 * Level 4: Logical Operators & Compound Statements
 * Question 40: Take a password string and check basic rules (length >= 8 and contains at least one digit).
 * 
 * Mentor Tips (Java 26):
 * - Check the length using len() (Python) / length() (Java). Check if at least one character in the string is a digit.
 */
public class Q40PasswordValidator {

    public static boolean solve(String password) {
        // TODO: Implement this method to solve the question
        false;
    }

    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        System.out.print("Enter password: "); String pw = scanner.next();\n        System.out.println("Valid: " + solve(pw));
        scanner.close();
    }
}
